

- [x] 713 ✅ 2024-05-13
- [ ] 862
- [ ] 395 


- [x] 报告管理 文字描述的标题去除 ✅ 2024-05-13




```ts

// 5. 联合分散可简化

type UppercaseA<str extends string> =

str extends 'a' ? Uppercase<str> : str

  

type UpercaseFirst<str extends string> =

str extends `${infer First}${infer Rest}`

? `${Uppercase<First>}${Rest}`

: str

  

type BEM<Block extends string, Element extends string[], Modifier extends string[]> = `${Block}__${Element[number]}--${Modifier[number]}`

  

type Combination<A extends string, B extends string> =

A | B | `${A}${B}` | `${B}${A}`

  

type AllCombinations<A extends string, B extends string = A> =

A extends A

? Combination<A, AllCombinations<Exclude<B, A>>>

: never;

  
  
  
  

type a = 'a'

type b = 'b'

  
  

type comAB = Combination<a, b>

type comAll = AllCombinations<'a' | 'b'>

  

type union = 'a' | 'b' | 'c'

  

type bc = Exclude<union, 'a'>

  

type str1 = 'normal'

type UppercaseStr1 = UpercaseFirst<str1>

  

type unionA = UppercaseA<union>

  

type str = `${union}++`

  

type bemResult = BEM<'guang', ['aaa', 'bbb'], ['warning', 'success']>;

```


```ts


// 6. 特殊特性

// isAny

type IsAny<T> = 1 extends (2 & T) ? true : false

  

type isAny1 = IsAny<any>

type isAny2 = IsAny<'1'>

// any & 任何 => any

  

// isEqual

type IsEqual<A, B> =

(<T>() => T extends A ? 1 : 2) extends (<T>() => T extends B ? 1 : 2) ? true : false

  

type a = 'a'

type b = 'b'

type neverType = never

  

type aequalb = IsEqual<a, b>

type isequalb = IsEqual<'b', b>

type isNever = IsEqual<b, never>

  
  

// isUnion

type isUnion<A, B = A> =

A extends A

? [B] extends [A]

? false

: true

: never

  

// [1 | 2] extends [1] // false

  

type ab = 'a' | 'b'

type isAb = isUnion<ab>

type isnotUnion = isUnion<string>

  
  

// isNever

// never & x => never

type IsNever<T> = [T] extends [never] ? true : false

  

type isNever2 = IsNever<never>

type isNotNever = IsNever<string>

  
  

// isTuple

// 元组

// type len = [1, 2, 3]['length'] // 3

// type len2 = number[]['length'] // number

type NotEqual<A, B> =

(<T>() => A extends B ? 1 : 2) extends (<T>() => B extends A ? 1 : 2) ? false : true

  

type IsTuple<T> =

T extends [...infer Eles]

? NotEqual<Eles['length'], number>

: false

  

type istuple = IsTuple<[1, 2, 3]>

type isNotTuple = IsTuple<string>

  
  

// UnionToIntersection ???

type UnionToIntersection<U> =

(U extends U ? (x: U) => unknown : never) extends (x: infer R) => unknown

? R

: never

type unionToIntersection = UnionToIntersection<{a: 1} | {b: 2}>

  
  
  

// getOptional

type GetOptional<Obj extends Record<string, any>> = {

[

Key in keyof Obj

as {} extends Pick<Obj, Key> ? Key : never

]: Obj[Key]

}

  

type hasOptionalType = {

name: string;

age?: number;

}

type getOptionalType = GetOptional<hasOptionalType>

  
  

// getRequired

type IsRequired<Obj extends Record<string, any>, Key extends keyof Obj> =

{} extends Pick<Obj, Key> ? never : Key

  

type GetRequired<Obj extends Record<string, any>> = {

[

Key in keyof Obj

as IsRequired<Obj, Key>

]: Obj[Key]

}

  

type getRequired = GetRequired<hasOptionalType>

  
  

// RemoveIndexSignature

type RemoveIndexSignature<Obj extends Record<string, any>> = {

[

Key in keyof Obj

as Key extends `${infer Str}` ? Str : never

]: Obj[Key]

}

  

type removeIdxSign = RemoveIndexSignature<{

[key: string]: any;

name: string;

}>

  
  

// ClassPublicProps

type ClassPublicProps<Obj extends Record<string, any>> = {

[Key in keyof Obj]: Obj[Key]

}

  

class Person{

public name: string;

private age: number;

protected size: number = 1;

constructor(name: string, age: number){

this.name = name

this.age = age

}

}

  

type classpublicprops = ClassPublicProps<Person>

  
  

// as const

const arr = [1, 2, 3] as const

  

type ReverseArr<Arr> = Arr extends readonly [infer A, infer B, infer C] ? [C, B, A] : never

  

type reversearr = ReverseArr<typeof arr>



```

基于已有报告内容的情报整编报告生成



1. 创建报告
（报告名称、数据源、事件名称、报告模版、报告主体）
![[Pasted image 20240513103444.png]]
2. 生成报告中
![[Pasted image 20240513095344.png]]
3. 生成报告完成
![[Pasted image 20240513095401.png]]

4. 查看报告
![[Pasted image 20240513144650.png]]




报告名称
数据源
事件名称
报告模版（默认模版/舆情模版）
报告主体（特定视角/立场）

数据预处理
大模型事件分析
报告生成


- 报告模版
	- 默认模版
		事件概述
		舆情热度走势
		舆论反应情况
		舆情风险研判
	- 舆情模版
		舆情概述
		舆情热度走势
		渠道声量分布
		舆论反应情况
		舆情风险研判
