


```ts

// 未解决 edge case

	// https://cn.vuejs.org/guide/essentials/reactivity-fundamentals.html#ref-unwrapping-as-reactive-object-property
	it('should synchronize state.count with ref count and allow replacing with a new ref', () => {
		const count = ref(0)
		const state = reactive({
			count, // count: count
		})

		// Initial values
		expect(state.count).toBe(0)
		expect(count.value).toBe(0)

		// Modify state.count and ensure synchronization
		state.count = 1
		expect(state.count).toBe(1)
		expect(count.value).toBe(1)

		// Assign a new ref to state.count
		const otherCount = ref(2)
		state.count = otherCount
		expect(state.count).toBe(2)
		expect(count.value).toBe(1)

		// Modify the new ref and check synchronization
		otherCount.value = 3
		expect(state.count).toBe(3)
		expect(count.value).toBe(1)
	})

```



```ts


toRef(value: any): any

toRef<T extends object, K extends keyof T>(object: T, key: K): IfAny<T[K], Ref<T[K]>, [T[K]] extends [Ref<any>] ? T[K] : Ref<T[K]>>

toRef<T extends object, K extends keyof T>(object: T, key: K, defaultValue: T[K]): IfAny<Exclude<T[K], undefined>, Ref<Exclude<T[K], undefined>>, [Exclude<T[K], undefined>] extends [Ref<any>] ? Ref<any> & Exclude<...> : Ref<...>>



function toRef(value){

isRef(value) ? value : ref(value)

}

```