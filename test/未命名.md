
```

test/it

BDD 行为驱动开发
it should xxx xxx

TDD 测试驱动开发


describe

测试套件 
相同行为的测试放在一起

describe.skip 跳过当前测试套件
describe.only 只执行当前测试套件

describe 可嵌套

expect

expect(1).toBe(1) // === 全等 用于值比较
expect().toEqual() // 用于对象之间比较
expect().toBeTruthy() // 真值
expect().toBeFalsy() // 假值
expect().toContain() // 是否包含 数组/string
expect().toThrow() // 抛出错误

setup & teardown

beforeEach
beforeAll
afterEach
afterAll

执行顺序：beforeAll beforeEach afterEach afterAll

filter


```


