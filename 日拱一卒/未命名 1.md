
unocss
https://icones.js.org/



yayi uie & file
1. 信息抽取 ie
非结构化文本：文本 pdf等
结构化：知识图谱 传统的数据库

ie 可拆分多个字任务（实体、关系、事件抽取）

关系先定义好后，进行关系抽取

问题：
- 不同的场景、任务、设置 需要构建 模型和数据集（数据集不能太少，并且需要多个类型）
- 能力复用、迁移难
- 成本高、周期长

通用大模型 需要微调（没有数据集，效果一般

已有IE：UIE、USM、InstructUIE





yayi file
溯源 - 钢铁AI管家 好像可以加

prompt规定json格式返回，之前有个分享也提到过，但因为是数据流接收，并在界面以打字机效果展示，在数据没有完全返回时，会在前端显示json的格式
解决：实际返回不展示，当数据在前端接收完毕，处理后再进行展示（没写



