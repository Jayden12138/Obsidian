```

ref()
	class RefImpl{
		get value(){}
		set value(){}
	}

reactive()
	return new Proxy(raw, {
		get
		set
		...
		ownKeys(){}
	})



1. render
	...props -> ownKeys() => 收集依赖 a

2. add props.foo
	ref-set
		trigger -> 找不到 foo 对应 dep

	？reactive-ownKeys
		收集依赖 a foo











```