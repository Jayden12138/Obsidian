>双指针

1. 合并两个有序链表
2. 合并k个有序链表
3. 寻找单链表的倒数第k个结点
4. 寻找单链表的中点
5. 判断单链表是否包含环丙找出环起点
6. 判断两个单链表是否相交并找出交点



```js

// 6.判断两个单链表是否相交并找出交点


// 160


while (p1 !== p2) {
	// p1 = p1?.next || headB
	// p2 = p2?.next || headA

	p1 = !p1 ? headB : p1.next
	p2 = !p2 ? headA : p2.next
}

// 画图理解 两种写法的差异

```








